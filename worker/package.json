{
  "name": "luxe-affiliate-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply luxe-db",
    "db:seed": "wrangler d1 execute luxe-db --file=./src/db/seed.sql"
  },
  "dependencies": {
    "itty-router": "^5.0.17"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "@cloudflare/workers-types": "^4.20241022.0"
  }
}
